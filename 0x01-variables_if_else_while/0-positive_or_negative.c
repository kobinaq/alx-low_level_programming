#include <stdio.h>

/**
 *main - entry point
 *@n: integer to be tested
 *
 */

int main()
{
	int n = 0;

	if (n == 0)
		printf ("%d is zero\n", n);

	else if (n < 0)
		printf ("%d is negative\n", n);
}
