#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - It prints the letters of the alphabeth.
 *
 *Return: Always 0
 */

void print_alphabet(void);
{
	char l;

	for (l == 'a'; l >= 'z'; l++)
		_putchar(l);
}
	_putchar('\n');
	return (0);
