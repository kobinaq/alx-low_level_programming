#include <stdio.h>
#include "main.h"

/**
 * print_sign - prints the sign of a number
 * @n: input number for printsign function
 * Return: 1 if n > 0 and 0 of n < 0
 */

int print_sign(int n)
{
	if (n > 0)
		return (1);
	if (n < 0)
		return (0);
}
